# Local virtualenvs — never needed in any image; each Dockerfile installs its own deps
**/venv/
**/.venv/
**/__pycache__/
*.pyc

# Git history
.git/

# Node
**/node_modules/

# Demo/test video data — mounted at runtime via docker/data volume, not baked into images
docker/data/

# OS/editor cruft
.DS_Store
*.swp
